---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: java
    tag: '8'

inputs:
  - name: pcf-ers-repo

outputs:
  - name: finalize-prod

run:
  path: pcf-ers-repo/ci/tasks/finalize-prod.sh

